
// Define status constants for deviceInfo type
export enum STATUS {
    GREEN,
    ORANGE,
    RED,
    BLACK,
    RECON,
    SMS,
}

// Define smoke tolerance
export const SMOKE_TOLERANCE = 384;
